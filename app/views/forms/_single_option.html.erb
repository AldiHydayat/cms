<%= f.hidden_field :question_id, value: question.id %>
<% question.options.each do |option| %>
  <div class="form-check">
    <%= f.radio_button :option_id, value: option.id, class: "form-check-input" %>
    <%= f.label :option_id, option.option_text, class: "form-check-label" %>
  </div>
<% end %>