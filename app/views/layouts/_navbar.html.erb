<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Navbar</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to("Home", root_path, class: "nav-link") %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to current_user.name, edit_user_registration_path, class: "nav-link" %>
          </li>
          <% if current_user.level == "admin" %>
            <li class="nav-item">
              <%= link_to "Form", forms_path, class: "nav-link" %>
            </li>
          <% end %>
        <% end %>
      </ul>
      <% if user_signed_in? %>
        <%= link_to 'Logout', destroy_user_session_path, class: "btn btn-outline-success mx-2 rounded-pill", method: :delete %>
      <% else %>
        <%= link_to 'Daftar', new_user_registration_path, class: "btn btn-outline-success mx-2 rounded-pill" %>
        <%= link_to 'Masuk', new_user_session_path, class: "btn btn-success mx-2 rounded-pill" %>
      <% end %>
    </div>
  </div>
</nav>