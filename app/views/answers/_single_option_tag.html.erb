<%= hidden_field_tag "answers[#{index}][question_id]", question.id %>
<% question.options.each do |option| %>
	<%= radio_button_tag "answers[#{index}][option_id]", option.id, required: question.is_required %>
	<%= label_tag "answers[#{index}][option_id]", option.option_text %>
<% end %>
